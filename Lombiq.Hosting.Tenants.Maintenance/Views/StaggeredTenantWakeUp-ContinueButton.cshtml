@using Lombiq.Hosting.Tenants.Maintenance.Constants

<a class="btn btn-success mb-1 @(Model.Part.IsFinished() ? "disabled" : string.Empty)"
   asp-action="Continue"
   asp-controller="Admin"
   asp-route-area="@FeatureNames.Maintenance">@T["Continue"]</a>
